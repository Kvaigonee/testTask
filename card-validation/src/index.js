import React from "react";
import ReactDOM from "react-dom";
import CardValidate from "./cardForm";

ReactDOM.render(
	<React.StrictMode>
		<CardValidate />
	</React.StrictMode>,
	document.getElementById("root")
);
